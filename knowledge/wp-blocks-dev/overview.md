# wp-blocks-dev

Plugin repo with built-in skills: https://github.com/phucbm/wp-blocks-dev

Install:
```shell
/plugin install wp-blocks-dev@phucbm
```

All skills (init, create-block, audit-blocks, audit-blocks-deep) and their usage are documented in the plugin's own `skills/` directory.
