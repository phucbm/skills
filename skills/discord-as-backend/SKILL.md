---
name: discord-as-backend
description: Use Discord as a lightweight backend for web apps — read a Forum channel as a structured database (bookmarks, link store) or push form submissions into a Discord channel via webhook. Trigger when the user wants zero-infra persistence using Discord, or wants a no-database form submission inbox.
when_to_use: When building a feature that needs persistence without a real DB — content store, bookmarks, notes — or a contact/report form, and Discord is already in the stack.
allowed-tools: Read, Edit, Write, Bash
---

Read [`@/knowledge/discord/web-patterns.md`](/knowledge/discord/web-patterns.md) and apply the relevant pattern.
